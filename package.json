{
  "name": "code-stink",
  "version": "1.0.0",
  "description": "A TypeScript code quality analyzer that measures the 'stinkiness' of your code",
  "main": "dist/index.js",
  "bin": {
    "code-stink": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/cli.js",
    "analyze": "ts-node cli.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "code-quality",
    "typescript",
    "static-analysis",
    "clean-code"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "ts-node": "^10.9.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
